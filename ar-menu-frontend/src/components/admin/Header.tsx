const Header = () => {
  return (
    <header className="h-16 bg-brand-dark-secondary border-b border- flex items-center px-6 flex-shrink-0">
      {/* Placeholder for breadcrumbs, profile menu, etc. */}
      <div className="text-lg font-semibold text-brand-text">Restaurant Dashboard</div>
    </header>
  );
};

export default Header;